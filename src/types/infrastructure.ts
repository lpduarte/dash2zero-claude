export interface InfrastructureData {
  municipality: string;
  chargingStations: number;      // Postos carregamento elétrico
  ecoPoints: number;              // Ecopontos reciclagem
  bikeStations: number;           // Estações bicicletas elétricas
  organicBins: number;            // Contentores orgânicos
}
